# 2026-09-16 输出清理记录

## 保留

- 源码、配置、构建目录和测试临时所需的最小运行环境。
- `outputs/formal_delay_stage1_20260916/`：Stage-1 正式矩阵根 manifest、MC 汇总、135 份
  已完成 `case_manifest.json`（保留每个 case 的配置、四条件、生产协议审计和状态）以及
  中断/清理审计；逐文件清理记录的唯一完整副本在根 `manifest.json`，case 文件中的重复副本
  已压缩。case 的原始输入与生产中间产物已在 compact evidence 生成后删除。
- `outputs/formal_evidence/`：compact 证据、manifest、当前阶段报告所需的高价值汇总。
- `outputs/track_delay_formal_4ch_local_20260916/` 根级 `manifest.json`、`track_branch_metrics.csv`、`track_protocol_payload_audit.csv`：旧 4ch production formal 的摘要仍被研究进展文档引用；其 raw seed 数据已清理。
- `outputs/track_delay_smoke_4ch_gpu_reaudit_20260916/` 根级 manifest/CSV：保留唯一一份与文档引用一致的 smoke 摘要。
- 旧 Stage-1/pilot 根级 manifest 和配置快照：仅用于源码身份、参数和失败/修复链路审计；case、MC、raw 目录已清理。

## 已删除的精确目标

- 旧 `track_delay_formal_4ch_local_20260916` 的 `seed_101/202/303` 原始输入、校准输入和派生产物（约 22.6 GiB）。
- 重复 smoke 的 `track_delay_smoke_4ch_gpu_reaudit_20260916/seed_101` 和完整重复目录 `track_delay_smoke_4ch_gpu_reaudit2_20260916`（约 2.3 GiB）。
- `stage1_delay_pilot_20260916*`、`track_delay_stage1_formal_20260916`、`track_delay_stage1_20260916_cuda` 中已被 compact evidence 或 manifest 替代的 `cases/`、`monte_carlo/` 原始/中间目录。
- 正式 Stage-1 135 个已完成 case 下的 `production/`、`scenes/`、空 `inputs/` 目录，以及
  与 `case_manifest.json.raw_cleanup` 完全重复的独立 `raw_cleanup.json`；保留的 case manifest
  仍包含配置、四条件、生产协议审计和清理摘要；原始文件路径、大小、哈希（已记录者）和
  删除状态的完整逐文件记录保存在根 formal manifest。
- `outputs/formal_evidence/stage1_delay_formal_sandbox_fail/`：未被文档引用、且被更完整的
  `stage1_delay_formal_sandbox_fail_all/` 覆盖的重复失败 compact 副本。
- `outputs/two_channel_error_observability_pre_metadata_20260915/`、
  `outputs/two_channel_error_observability_pre_near_confounded_20260915/` 和五个
  `unknown_system_error_geometry_v2_*_smoke_20260914` 目录（删除前 `du -sh` 合计约
  17.5 MB）：均为未被当前文档引用的 pre/smoke 试跑，已有正式/compact 结果替代。
- 135 份 standalone case manifest 中与根 manifest 完全重复的 34,830 条逐文件清理记录：
  case 文件仅保留记录数、删除字节数、状态和“已汇总至根 manifest”标记；根 manifest 保留
  每条路径、大小、哈希和删除状态，避免重复占用空间并保持可审计性。
- 两个 interrupted case 中未被 compact evidence 或报告引用的 22 个
  `paired_range_phase_{raw,csi}_period050_beam050.f32` debug 数组（`360,448` bytes）；
  保留同一失败现场的 CSV、scene manifest 和 `raw_cleanup.json`。
- 工作树 `.tmp_pytest/`：可由测试重新生成的 pytest 临时文件。
- `/tmp` 下已核对属于本项目的旧 `pgrcc*` 临时目录和配置检查文件（旧 smoke/formal/observability 临时输入及旧 source staging）；不影响当前源码或正式输出。

## 清理理由与状态

这些目标均为可再生原始输入、重复运行、已失败且已有 compact 失败证据的现场、重复逐文件
清理记录、未引用的 pre/smoke 试跑或测试临时文件；保留内容足以支撑当前 formal 结果分析、
源码/配置身份和报告审计。
正式矩阵已由 analyzer 收敛为 `outputs/formal_evidence/stage1_delay/` 的九个文件，源 manifest
确认 135/135 case `completed`、MC 135 行且每格 100 trials `passed`；formal 根目录中 `.bin`
残留为 0。当前 `outputs/` 约 491 MB（其中 formal case 的生产协议审计仍保留）、可见 `/tmp`
约 106 MB，根分区可用空间约 58 GB；项目自身旧 `/tmp/pgrcc*` 已清理，未触碰系统私有目录。
删除的正式原始/中间产物不可由本地文件直接恢复，但其必要路径、大小、哈希和删除状态仍
保存在根 formal manifest 及 compact manifest 中；case manifest 的配置和协议审计仍可直接复核。
九文件中的 `id_switch_audit.csv` 是在删除 TrackManager 原始 CSV 前生成并保留的最终审计
副本；清理后重新运行 analyzer 时不能凭空重建该列，必须使用这份 compact evidence，避免
把缺少原始关联文件的空审计结果误报为通过。
