# 2026-09-17 深度清理与 Formal-v1 只读重分析记录

## 清理边界

本次清理只删除了已确认无引用、可由源码/manifest 重建或明显为重复试跑的对象。Formal-v1
历史资产按项目规则保持不可覆盖：

- `outputs/formal_delay_stage1_20260916/` 的根 manifest、case manifest、失败现场和配置快照；
- `outputs/formal_evidence/stage1_delay/` 的九文件 compact evidence；
- 当前 Stage-2 配置、观测性结果、生产链摘要和已有报告。

## 已删除的精确目标

- `outputs/pure_spatial_dof_smoke_20260915/`；
- `outputs/pure_spatial_dof_smoke_v2_20260915/`；
- `outputs/stage1_delay_pilot_20260916_v2/`；
- `outputs/stage1_delay_pilot_20260916_v3/`；
- 本轮产生的 `scripts/__pycache__/`、`tests/__pycache__/` 和 `/tmp` 下三个本轮临时对象：
  `pgrcc_closure_cli.json`、`pgrcc_track_selftest.log`、
  `gmti_track_output_source_selftest_82/`。

两份仍被文档/脚本引用的 servo 产物没有删除；它们不属于本次“无引用重复物”的安全删除集合。

## 保留理由

Formal-v1 的 135 个 case manifest 含配置、四条件审计和清理状态，根 manifest 保留逐文件删除
记录；compact evidence 是汇报和复现入口。原始二进制已在历史清理中删除，本次不重新生成或
覆盖它们。当前 Stage-2/观测性目录虽有未跟踪文件，但仍被下一阶段设计和报告使用，暂不删除。

## Formal-v1 ID-switch 只读重分析

输入：`outputs/formal_evidence/stage1_delay/id_switch_audit.csv`，1040 条既有 switch。
输出：`outputs/formal_evidence/stage1_delay_v2_reanalysis_20260917/`。

重分析只重标已有行，不从 truth、detection 或 raw 文件创建候选/新 switch，也没有修改 v1
输入。由于原始 TrackManager v2 association stream 已在历史清理中删除，1040 条全部标记为
`unclassifiable_production_fields`，缺失字段计数为
`track_association_audit_v2.csv: 1040`；这不是“没有机制”，而是“当前保留证据不能归因”。

复现入口：

```bash
python3 -c 'from pathlib import Path; from scripts.audit_delay_track_id_switch import reanalyze_retained_audit; reanalyze_retained_audit(Path("outputs/formal_evidence/stage1_delay/id_switch_audit.csv"), Path("outputs/formal_evidence/stage1_delay_v2_reanalysis_20260917/id_switch_audit_v2_reanalysis.csv"))'
```

## 2026-09-20 Formal-v2 深度清理补记

Formal-v2 的 135/135 case 已完成，compact evidence 和分层统计生成并校验后，删除了精确的
raw case 树 `outputs/formal_delay_stage1_v2_20260917/`，回收约 7.85 GB（删除前 `.bin` 已为
0；主要是可重建的生产 CSV、PNG、逐分支 JSON、日志和中间图）。保留的高价值记录为：

- `outputs/formal_evidence/stage1_delay_v2_full_20260917/`：compact CSV、manifest、15 MB
  Formal-v2 run manifest、135 个 case manifest 的 28 MB 压缩归档、retention manifest；
- `outputs/formal_evidence/stage1_delay_v2_full_20260917_hierarchical/`：15 个 physical scene
  block、9 个 delay 的分层统计；
- `outputs/formal_evidence/stage1_delay/`、v1 reanalysis、v1 exploratory hierarchy 和相关
  配置/报告：均为当前结论或不可覆盖的历史证据。

同时删除了已被完整 v2 替代且无文档引用的三个 compact 试跑目录：
`outputs/formal_evidence/stage1_delay_v2_20260917/`、
`outputs/formal_evidence/stage1_delay_pilot_v2/`、
`outputs/formal_evidence/stage1_delay_pilot_v3/`。上述删除均为明确路径，原始 v2 case manifest
已先归档；compact 证据和报告仍可直接复现当前结论。

## 2026-09-20 继续清理补记

已删除 8 个经逐项检索确认无当前文档、脚本或测试引用的旧 pilot/重复 oracle 目录，合计约
`167,180` bytes：

- `outputs/support_routing_oracle_pilot/`；
- `outputs/support_routing_oracle_pilot2/`；
- `outputs/pfa_closure_formal_20260914/`；
- `outputs/ai_csi_model_mismatch_oracle_formal_seed2026091011/`；
- `outputs/ai_csi_model_mismatch_oracle_formal_seed2026091012/`；
- `outputs/ai_csi_model_mismatch_oracle_formal_seed2026091013/`；
- `outputs/clutter_only_servo_formal_compact_20260914/`；
- `outputs/clutter_only_servo_smoke_boolfix_20260914/`。

Formal-v1 的 `outputs/formal_delay_stage1_20260916/cases/` 和 `interrupted_cases/` 未删除：现有
审计文档仍把它们标为历史审计资产/失败现场，且当前授权未明确覆盖这类不可逆删除。根
`manifest.json`、配置/模板、MC 汇总、compact v1/v2 evidence、分层统计、失败审计和报告继续保留。
如需进一步回收这两棵树，必须先取得对这两个精确路径的明确批准，再更新本记录中的保留边界。

随后完成了更窄的可逆审计清理：两棵树中的 CSV、PNG、日志和数值中间文件共 `560` 个、
`50,337,460` bytes 已删除；JSON/XML 记录未删除。删除前先将两棵树中的 `320` 个 JSON/XML
记录压缩归档为
`outputs/formal_evidence/stage1_delay_v1_audit_records_20260916.tar.gz`（约 28 MB，SHA-256
`88e37a6533dc5870ae4c7f9b1091a4a8a179d631252818f5a17033f8bd201e23`）。当前 v1 原始树仅保留
manifest/config/XML 记录和根 manifest；原根 manifest SHA 未改变。
