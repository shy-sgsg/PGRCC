# 《测试问题记录》答复（基于当前 GMTI 资料）

以下结论按当前源码、测试大纲、当前配置和已有运行产物整理。MMTI 部分的参数来自仓库中的 MMTI 测试大纲；当前 GMTI 工程本身不包含可直接运行的 MMTI 主程序，因此不能把 MMTI 参数写成 GMTI 源码已经验证的结果。

## 1. GMTI/MMTI 仿真数据是怎么生成的？

GMTI 目前采用分级数据路线：

1. **Stage1：实测 IQ 工程验证数据。** 对旧系统双通道 IQ 做波位镜像/插值、慢时间重采样和距离向 FFT 扩展，生成新协议数据。它适合验证数据接入、检测、定位、航迹和实时性，不适合直接评价宽角 DBS 几何。
2. **Stage2：新系统参数自洽的统计杂波仿真。** 按平台位置、目标/散射体位置、波位角和脉冲时刻计算斜距 (R(t))，由
   `τ=2R(t)/c-sample_delay`
   得到快时间位置，再叠加 LFM 回波、载频相位、慢时间运动相位、通道相位、面杂波、强散射点、线状散射体和复高斯热噪声，输出新协议 IQ 和逐脉冲 truth。
3. **Stage3：场景反演与物理正演。** 从 SAR/DBS 场景提取地面散射单元，再按新系统平台轨迹、波位、传播距离和通道几何重新正演，主要用于宽角 DBS 和复杂背景验证。

当前 GMTI 五周期正式数据的主要参数为：61 个物理波位（−60°～60°）、每波位 128 脉冲、PRF 1300 Hz、四通道交织 int16 IQ、每通道 11820 点并补零到 11840 点、50 MHz 带宽、平台高度 6 km、速度 60 m/s。仿真同时输出目标 truth、场景 truth、配置快照和数据校验信息。

## 2. 虚警率大于 10% 的问题

虚警应定义为“一对一 truth 匹配后未匹配的检测数”，虚警率按

```text
false_alarm_rate = false_alarm_count / detection_count
```

统计，检测率则按 `matched_count / truth_count` 统计。

当前证据显示该问题确实存在，但与运行配置有关：

- 2026-07-29 动态 CSI 检测带基线：44/50 匹配，49 个检测中 5 个虚警，虚警率 **10.20%**；
- 2026-07-31 `csi_detection_band_mode=union`：44/50 匹配，93 个检测中 49 个虚警，虚警率 **52.69%**；
- 另一次基线为 4/45，虚警率 8.89%。

因此，“不同机器有随机性”只能解释小幅波动，不能解释 `union` 配置下从约 10% 增加到 52.69%。当前主要嫌疑是 union 分支将不同 CSI 检测带的候选合并后扩大了检测单元集合，再叠加统计杂波/强散射背景；问题闭环记录也明确提示 union 可能提高虚警，尚不能写成已解决。

## 3. 为什么分辨率变化较大？

GMTI 50 MHz 波形的理论距离分辨率为：

```text
ΔR = c / (2B) ≈ 2.998 m
```

正式测试应使用孤立点目标脉压后的线性功率剖面，以峰值半功率（−3.0103 dB）左右交点的距离差作为 3 dB 主瓣宽度，判据为不大于 5 m。

当前结果变化较大，主要可能来自：

- 目标信杂噪比较低，单脉冲剖面不稳定；
- 在杂波背景中测到的是“有效主瓣宽度”，不是无杂波条件下的本征波形分辨率；
- 目标在脉冲积累期间存在距离徙动、波束增益变化或相位不完全一致；
- 32/64 点相干积累提高了检测信噪比，但会改变实际测量所使用的慢时间/目标支撑条件；
- 通道相位补偿、脉压峰选取和插值方法仍可能引入小误差。

所以，32/64 点积累不应直接解释为“物理距离分辨率一定变差”。应分别报告：无杂波高 SNR 单点的本征 3 dB 分辨率，以及复杂背景下检测目标的有效主瓣宽度。

## 4. GMTI 方位向定位误差是多少？

当前评估脚本实际统计的是地面二维位置误差：

```text
position_error_m = hypot(e_detection-e_truth, n_detection-n_truth)
```

它不是单独的方位向误差。因此，“GMTI 方位向误差约 60 m”不宜直接作为严格表述。按 2026-07-29 基线的 44 个匹配样本，二维位置误差为：均值 **12.80 m**、P95 **30.89 m**、最大 **60.24 m**。`union` 配置下对应为均值 **13.61 m**、P95 **39.72 m**、最大 **70.25 m**。

建议报告写成“地面二维定位误差最大约 60 m（特定 union 配置最大约 70 m）”。若要回答纯方位向误差，还需沿目标视线的横向坐标单独计算，当前结果文件尚未提供该独立指标。

## 5. 测试报告中的误差如何定义？

- **距离向定位误差：**
  `range_error = range_detection - range_truth`；报告中同时给有符号误差和绝对误差统计。
- **方位/地面位置误差：**
  由检测点与 truth 的坐标差计算；二维位置误差为 `hypot(Δe, Δn)`，不能把它简称为方位误差。
- **速度误差：**
  `velocity_error = radial_velocity_detection - radial_velocity_truth`，只比较径向速度，不把平面速度模长混进来。
- 误差只对一对一匹配样本统计；漏检和虚警必须单独列出，不能用成功样本平均值掩盖。

当前 2026-07-29 基线匹配样本的距离绝对误差均值 **0.50 m**、P95 **1.19 m**、最大 **1.24 m**；但只有 44/50 个 truth 被匹配，报告必须同时写明漏检 6 个。

## 6. 最小可检测速度如何判定？

当前专项样本中有一个地面参考径向速度约 **4 m/s** 的目标，约等于 **14.4 km/h**，其余目标速度约为 4.4～6.0 m/s。4 m/s 是接近 15 km/h 指标的边界样本，不等于已经完整测出了最小可检测速度。

严格的最小可检测速度应在固定背景、SNR/RCS、通道误差和检测门限下进行速度梯度实验，并预先定义：

```text
MDV_Pd80 = 满足 Pd≥80% 的最小径向速度
MDV_Pd90 = 满足 Pd≥90% 的最小径向速度
```

因此，当前可以说“4 m/s 边界目标用于验证低速目标链路”；若验收指标严格写成 15 km/h，应另设置约 4.167 m/s 的目标，并报告速度扫描结果，而不能仅凭一个 4 m/s 样本宣称已达到最小可检测速度。

## 7. 83 km 和幅宽是什么关系？

GMTI 的“83 km”是脉压截取后的**近端斜距**，不是幅宽。当前参数为 `sample_delay=488 μs`、`fs=60 MHz`、`range_crop_start=3864`、`range_crop_len=4096`：

```text
距离采样间隔 = c/(2fs) ≈ 2.498 m
近端斜距 = c/2 × (488 μs + 3864/60 MHz) ≈ 82.803 km
远端斜距 ≈ 93.036 km
斜距向覆盖宽度 ≈ 10.233 km
```

按 6 km 平台高度换算到地距，覆盖约 **82.585～92.842 km**，地距宽度约 **10.257 km**。因此测试大纲中的“距离向测绘带宽不小于 7 km”与当前约 10.2 km 的仿真截取窗口是相容的。

还要区分波束覆盖：单个 2.28° 波束在 83～93 km 处的横向地面覆盖约 3.3～3.7 km；61 个波位的 −60°～60° 全扫描横向包络约 143～161 km。这些是不同的“宽度”概念，报告应明确写“距离向斜距/地距幅宽”还是“全扫描横向包络”。

## 8. MMTI 分辨率为什么差别较大？

按当前 MMTI 测试大纲，信号带宽按 20 MHz 计算时理论距离分辨率为：

```text
c/(2B) ≈ 7.495 m
```

工程判据为距离向 3 dB 分辨率不大于 10 m。MMTI 单脉冲目标不一定能被检测，使用 32/64 点相干积累是为了提高检测信噪比；在理想静止点目标条件下，慢时间积累不应直接改变快时间匹配滤波的本征分辨率，但在实际测量中会受到目标距离徙动、波束权重、相干性、噪声和主瓣交点选取影响。

正式报告应使用固定多普勒频点、连续复数距离剖面测量 3 dB 宽度，并分别给出本征分辨率和含杂波/含积累条件下的有效宽度。当前 MMTI 大纲中“等效噪声带宽 30 MHz”和表格中的“20 MHz 信号带宽、30 MHz 采样率、50 MHz DDC 等效噪声带宽”存在口径不一致，最终报告前应以实际 MMTI XML/波形配置确认 `B`，不能混用采样率、噪声带宽和信号带宽。

## 9. MMTI 的距离和幅宽如何对应？

按当前 MMTI 测试大纲的斜距口径：

- 接收延迟 340.1 μs 对应最近斜距约 **50.98 km**；
- 采样窗末端 1633 μs 对应约 **244.8～245.8 km**；
- 有效幅宽时间约 1293 μs，对应斜距幅宽约 **193.8 km**。

因此，测试指标中的“作用距离不小于 240 km、距离向测绘带宽不小于 194 km”应分别理解为远端作用距离和有效距离向覆盖宽度。`crop_start=5100` 是脉压边界完整性裁剪参数，用于去除不完整脉压边界，不能直接当成新的物理作用距离或幅宽起点。

## 可核查证据

- 仿真路线与参数：`docs/仿真测试/GMTI新系统仿真数据生成计划v3.0.docx`、`docs/代码说明/simulator_场景与目标注入说明.md`
- GMTI 测试定义：`docs/功能测试与实时性测试/无人机载多功能雷达实时处理算法软件模块测试大纲_GMTI_MMTI补充版.docx`
- GMTI 虚警基线：`outputs/manual_run/gmti_complete_test/20260729_225758/reports/truth_eval/summary.json`
- GMTI union 虚警结果：`outputs/manual_run/gmti_csi_band_ab_20260731/union_pf1e7_low22/reports/truth_eval/summary.json`
- 虚警问题闭环：`docs/仿真测试/问题闭环/issue_register.csv`
- 当前运行时配置：`outputs/manual_run/gmti_csi_band_ab_20260731/union_pf1e7_low22/config/gmti.xml`
