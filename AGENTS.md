# AGENTS.md

本文件约束本仓库及其全部子目录中的后续代理工作。子目录如有更具体的
`AGENTS.md`，仅可补充该子树的特殊要求；除非明确说明，不得削弱本文件中的
正确性、验证、可复现性和工作区安全要求。

本文件是长期工作规范，不是当前任务清单、阶段实施计划或历史实验记录。
一次性场景、固定样例、阶段编号、临时阈值和某次运行的验收数字，应记录在
对应任务文档和实验产物中，不应固化为本文件的永久约束。

## 1. 总目标

代理的职责是把用户当前任务推进到“已实现、已运行、已验证、可复现、可审查”
的状态，而不是只给建议、只改代码、只编译，或复述历史实验。

长期遵守以下原则：

1. 用户当前消息和当前任务说明优先于旧计划、旧报告和旧实验结论。
2. 历史材料是上下文和回归参考，不是默认待办，也不是永久验收标准。
3. 结论必须来自当前源码、当前配置、当前输入和本次实际运行证据。
4. 用户要求“修复、完成、跑通、补实验”时，应主动实施并验证，直到成功或出现
   无法在现有权限和材料下消除的明确阻塞。
5. 不得把未运行写成通过，不得隐藏失败、退化、假警、漏检、性能异常或未验证边界。
6. 默认用中文记录实验和交付；代码命名、协议字段和接口风格遵循项目现状。

## 2. 恢复任务与确定当前主线

每次开始工作先执行：

```bash
git status --short --untracked-files=all
```

随后根据当前任务读取最小但充分的上下文：相关源码、配置、最近产物、对应代码
说明、用户指定的任务文档。搜索优先使用 `rg` 和 `rg --files`。

当用户说“继续”时：

1. 从当前对话、现有计划、工作树改动、最近生成的日志和文档恢复尚未完成的目标；
2. 检查上次停止点和失败原因，不从头盲目重复已经完成的工作；
3. 优先执行仍会影响最终结论的实验、修复和验证；
4. 若多个历史任务并存，以用户最近明确指定的目标为主；不要擅自复活已完成的旧阶段。

只有缺失信息会实质改变实现方向、外部接口或验收含义时才询问用户。可从代码、
数据、配置或日志查明的内容，应先自行查明。

复杂任务应维护可执行计划。计划描述当前要解决的问题和验证闭环，不应抄录大段
历史需求；完成一项即更新状态，发现根因变化时同步调整。

## 3. 实施纪律

### 3.1 先建立可信基线

修复或优化前尽量复现当前问题，并保存命令、配置、退出码、关键日志、输入身份和
失败产物。完整链路成本高时可以先做最小复现，但最终必须回到代表性的真实链路。

### 3.2 找根因，不用调参掩盖问题

优先沿数据流、控制流、配置来源、单位、布局、生命周期和并发关系定位根因。修改
门限或容差时必须说明依据，并用 A/B 或回归实验证明它没有只是隐藏错误。

### 3.3 修改最小但闭环完整

- 修改范围围绕根因，避免无关格式化、重命名和清理。
- 同一语义在 CPU/CUDA、文件/实时输入、仿真/生产、配置/运行时记录中应保持一致。
- 新逻辑应具备边界检查、明确错误信息和适用的自动化测试。
- 修复一个分支时检查相邻路径，防止 fallback、兼容路径或默认值悄悄改变行为。
- 不得靠硬编码某次实验的样本数、波位数、尺寸或阈值完成当前功能；应由配置、协议
  或输入元数据推导。

### 3.4 保持接口和兼容性

除非用户明确要求改变接口：

- 保持已有命令、结果、文件格式、二进制布局和默认运行方式兼容；
- 新能力应有明确开关，旧模式仍可运行；
- 协议事实、当前实现和推测必须区分，遇到冲突要记录并验证，不能自行发明字段；
- PIPE 结果协议中的每个目标必须由“已确认航迹在当前处理周期实际关联到的检测”触发
  并可追溯到该检测。不得改成全部原始检测、Tentative 航迹、未命中的 Coasted 预测点
  或历史周期检测；默认载荷使用本周期检测字段，只有用户显式配置的状态源才可改用
  预测/滤波取值，且不得放宽当前周期命中门槛，必须记录逐字段来源；原始检测 CSV/
  诊断输出只能作为离线审计旁路，不能改变协议目标选择语义；
- 航迹全流程验收优先复用生产 `TrackManager` 已有的 `track_debug`（帧、关联 detection、
  航迹状态）和输出载荷审计文件，不得另写简化跟踪器替代它；逐周期 detection 快照只
  用于 truth 匹配和逐字段交叉验证。正式运行需使用保留诊断输出的运行模式，并验证
  每条协议载荷都能反查到同周期 `Confirmed + matched_this_frame` 的航迹和关联 detection；
- 变更配置字段时同步默认值、解析、校验、派生量、运行时快照、示例和文档；
- 变更输出 schema 时同步消费者、评价脚本和字段说明，必要时显式版本化。

## 4. 验证规则

验证强度与改动风险相称，通常按以下顺序执行：

1. 定向编译、静态或语法检查；
2. 单元测试和 selftest；
3. 问题的最小复现；
4. 代表性端到端链路；
5. 与修改相关的兼容和异常场景；
6. 性能、稳定性或设备实验。

低层测试通过不能替代高层链路。程序退出码为 0、产物文件存在或某个中间函数
返回 valid，也不能单独证明算法和物理结果正确。

验收指标必须来自当前任务。对于带 truth 或参考结果的任务，应检查适用的完整结果，
包括正确匹配、遗漏、额外输出、数值误差和单位/符号/坐标系；不要只挑成功样本，
也不要把旧实验的固定数字写成通用门槛。

数值比较须事先定义与算法精度相称的容差。不能在看到结果后为使测试通过而临时
放宽容差，也不能对存在合理浮点非确定性的并行程序强求无依据的 bitwise 一致。

异常路径至少覆盖与本次修改直接相关的缺失输入、尺寸或长度错误、配置非法、资源
不足、中断/重启和并发状态；系统不得静默填充、覆盖有效数据或把不完整结果当成功。

## 5. 构建与代码检查

默认原生 CUDA Release 构建：

```bash
cmake -S . -B build -DCMAKE_BUILD_TYPE=Release
cmake --build build -j4
```

C++/CUDA 实质修改交付前至少运行适用的构建、测试和差异检查：

```bash
cmake --build build -j4
ctest --test-dir build --output-on-failure
git diff --check
```

修改 Python 或 Shell 后运行相应检查：

```bash
python3 -m py_compile scripts/<changed_script>.py
bash -n scripts/<changed_script>.sh
```

定向构建用于快速迭代，最终仍需执行与风险相称的整体检查。不得混用原生 CUDA 与
其他工具链的构建目录；不得未经授权安装系统依赖、使用 `sudo` 或修改系统 CUDA。

## 6. CUDA 和稀缺运行额度

需要 GPU 才能证明结果的任务，必须在真实 CUDA 设备上运行。CPU smoke、仅编译或
代码审查不能替代 CUDA 实验。

若当前具有受限提权或 GPU 运行额度：

1. 在代码达到可运行状态后尽早执行最关键的 CUDA 探测和实验，不要把 GPU 验证
   全部拖到任务末尾；
2. GPU 实验运行期间并行整理文档、日志和 CPU 侧分析，不浪费可用窗口；
3. 若设备在沙箱中不可见或运行因权限失败，应立即按环境规则请求受限提权并重试；
4. 首轮先运行能最快暴露环境、输入和主链路问题的代表性 case，再扩展完整矩阵；
5. 额度有限时优先保证结论关键路径，次要绘图和文字整理可后置。

提权额度、GPU 配额或临时设备权限不足只阻塞依赖该资源的具体实验，不得阻塞后续
仍可安全完成的代码、CPU 测试、数据分析、文档和审计工作。遇到此类失败时，应记录
待补命令、输入、预期产物、失败原因及允许重试时间，标记为 GPU 待补项，然后继续
推进其余目标；不得绕过明确的额度限制，也不得在允许时间前无诊断地反复申请。

每次任务恢复运行时，优先检查 GPU 待补清单和当前额度：先补齐所有需要提权/GPU 的
既有实验与测试；若 GPU 窗口仍可用，应把当前任务后续可合理预见、代码已具备运行条件
的 GPU 回归和性能实验一并完成并留存证据，再转入不依赖 GPU 的工作，避免把关键设备
验证拖到额度耗尽之后。

运行前检查并记录：

```bash
nvidia-smi
```

性能结果必须注明 GPU 型号、供电状态、P-state、频率、功耗、温度和负载。电脑未接
电源、限功率、热降频或明显受后台负载影响的结果不得作为正式基准。正式性能对比
应先 warm-up，至少重复 3 次，保留各次结果并报告稳定统计量，同时验证输出正确性。

计时应区分 kernel、传输、CUDA 路径 wall time 和完整流程 wall time；不同口径不能
混算。没有实际 CPU reference 时不得声称 CPU/CUDA 一致性已经通过。

## 7. 配置、数据和运行时状态

- 配置应有单一、可追踪的来源优先级；避免在循环或每周期重复解析静态配置。
- 派生量应集中计算和校验，避免在不同模块重复推导出不一致结果。
- 并发处理使用的配置应绑定不可变快照或等价机制，不能在运行中读取一半新一半旧。
- 输入缓冲区的所有权和生命周期必须覆盖消费者全部使用期；不得覆盖仍在处理的数据。
- 大输入优先使用 view、预分配或流式处理，避免无意义的整周期复制和热点动态分配。
- 实时或分块输入必须正确处理任意切分、半包、多包、丢失、乱序和重新同步；不能
  假设一次读取恰好对应一个逻辑单元。
- 单位、字节序、通道顺序、复数 I/Q、符号和坐标系应在边界处明确并检查。

## 8. 证据、产物与复现

不要覆盖仍有审计价值的基准和失败现场。正式运行使用独立、含义明确的输出目录。

可复现实验至少记录：

1. 工作目录、完整命令、参数和退出码；
2. 配置路径、关键 override 和运行时实际配置；
3. 源码提交身份与 dirty worktree 状态；
4. 关键输入的路径、大小和 SHA-256；
5. CPU/GPU、驱动、构建类型和重要环境状态；
6. 日志、manifest、计时、核心输出和评价产物；
7. 预期、实际结果、判定依据、限制和未验证事项。

不同输入、配置、源码、设备状态或计时口径的结果不能直接横向比较，除非报告明确
把差异列为实验变量。

`outputs/`、`docs/`、CSV 或日志可能被 `.gitignore` 忽略。`git status` 看不到不代表
没有生成或修改；交付时应直接检查文件。不要批量纳入大型输出、原始数据或无关历史
文档。

## 9. 文档与交付标准

行为性改动应同步当前代码说明；完成实验应保存复现命令和本次结果；新增重要文档时
更新文档导航。历史报告保留其时间、输入和代码身份，不为迎合旧数字而回写结论。

用户要求完整文档交付时，应区分：

- 导航/阅读顺序：每份文档和关键目录的用途及推荐顺序；
- 汇报报告：面向老师或决策者，突出目标、方法、结果、证据和限制；
- 审计报告：面向复现者，事无巨细记录环境、命令、配置、输入、产物、判读方法和
  已知问题。

最终回复以结果开头，至少说明：

1. 修改和完成了什么；
2. 实际运行了哪些验证；
3. 关键结果及证据路径；
4. 未完成、失败、限制或未验证内容；
5. 用户可直接复制执行的最短复现入口。

不要让用户从大段日志中自行猜测是否成功，也不要用“理论上可行”“应该通过”代替
实际运行结果。

## 10. 工作区与 Git 安全

工作树中的现有改动可能属于用户或其他任务：

- 不得执行 `git reset --hard`、`git checkout --` 或等价破坏性操作；
- 不得删除、覆盖、回退或格式化与当前任务无关的文件；
- 修改前检查相关 diff，重叠时保留用户意图；
- 不得擅自提交、推送、创建 PR 或改写历史；
- 删除大型产物、修改忽略规则、安装依赖或扩大外部影响前须取得明确授权；
- 使用非交互式命令，并保持每项改动可审查。

## 11. 自主性与沟通

工作过程中提供简短进度，说明当前是在审查、复现、修改、运行还是分析。不要因任务
耗时而静默，也不要用频繁确认代替推进。

出现失败时应读取真实错误、修复后重跑；同一命令反复失败却没有新诊断不算推进。
只有权限、缺失输入、外部设备或会改变用户目标的选择确实阻塞时才停下，并明确说明：
已经检查了什么、阻塞证据是什么、需要用户提供什么。

在任务真正完成前，只要仍有安全且在范围内的修复、验证、文档或审计工作可以做，
就继续推进。

`/goal` 或其他持续目标只有在用户目标真实完成时才可结束。不得仅因提权/GPU 额度
暂时不可用、某个设备实验待补或一次权限申请失败，就把仍可推进的目标随意停止、视为
完成或提前交付；应按本文件第 6 节记录待补项并继续，待额度恢复后优先补跑。只有所有
安全的非阻塞工作均已穷尽且确有外部条件完全阻断推进时，才按上级运行规则报告阻塞。

## 12. 磁盘空间与进度沟通

实验、构建和诊断必须遵守“保留必要记录和高价值结果”的原则：

1. 开始长时间运行前检查工作区和目标输出目录的空间占用；实验过程中优先使用
   独立、含义明确的输出目录，避免覆盖仍有审计价值的结果。
2. 只保留能支持结论、复现、审计或定位失败的配置、manifest、日志、汇总表、核心
   图和代表性原始结果；无用的中间产物、smoke 实验、探索性尝试和重复数据应在
   确认不再需要后及时删除，不得为了“留痕”无限累积。
3. 删除前先核对明确的目标路径和文件用途，不得使用面向工作区根目录的递归删除，
   不得删除用户已有资料、唯一失败现场、输入数据或尚未汇总的证据。必要时先将
   高价值结论汇总到可复现报告和 manifest，再清理派生产物。
4. 每次清理记录保留内容、删除内容及其理由；磁盘不足时优先清理低价值中间结果，
   不得通过降低验证范围或覆盖基准结果来节省空间。
5. 长任务在审查、修改、运行、分析和清理等关键阶段及时更新进展；每次更新说明
   当前阶段、已完成的实际证据、结果或失败原因、剩余风险和下一步动作。运行时间
   较长时不得长时间静默；外部设备或权限等待也要报告已确认的状态和待补命令。
6. 进展描述必须区分“已运行并有产物”“已检查但未运行”“待补/受阻”，不得把计划、
   文件存在或退出码 0 单独写成算法结论。

## 13. 交付前检查清单

- [ ] 当前目标来自用户最新要求，而不是历史阶段惯性。
- [ ] 已检查并保护工作树中的既有修改。
- [ ] 已建立可信基线或解释为何无法建立。
- [ ] 修改围绕根因，兼容路径和配置语义已同步检查。
- [ ] 构建、测试、端到端链路和异常场景按风险实际运行。
- [ ] 涉及 GPU 时已尽早使用真实 CUDA 设备，并记录有效的设备状态。
- [ ] 关键结论有当前产物和可追溯指标支持。
- [ ] 命令、配置、输入、环境、结果和限制足以复现与审计。
- [ ] 相关代码说明、实验记录和导航已按需更新。
- [ ] `git diff --check` 通过；所有未完成项均明确披露。
